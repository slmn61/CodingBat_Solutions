public int sum2(int[] nums) {
  int sum = 0;

  if(nums.length > 1){
    for(int i = 0; i < 2; i++){
      sum = sum + nums[i];
    }
  }else if(nums.length == 1){
    sum = nums[0];
  }
  return sum;
}
