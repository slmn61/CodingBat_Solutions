public String lastChars(String a, String b) {
  String na = "@";
  String nb = "@";

  if(a.length() != 0){
    na = a.substring(0,1);
  }

  if(b.length() != 0){
    nb = b.substring(b.length()-1, b.length());
  }

  return na+nb;
}
